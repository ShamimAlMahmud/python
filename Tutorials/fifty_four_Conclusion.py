from fifty_five_conclution import do_a_thing, doing_another_thing

do_a_thing()

doing_another_thing()

#We can use the star(*) sign instead of write do_a_thing, doing_another_thing
#If we want take one or more than one file run together then instead of using their name we can use only star sign
